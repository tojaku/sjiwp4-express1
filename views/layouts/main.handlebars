<!DOCTYPE html>
<html>

<head>
    <title>Moja Express aplikacija</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    {{#if user}}
        <div class="p-2 bg-base-200 flex flex-row justify-end items-baseline gap-2">
            <div>{{user.name}}</div>
            <a href="/users/signout" class="btn btn-sm btn-accent">Odjava</a>
        </div>
    {{/if}}

    {{{body}}}
</body>

</html>